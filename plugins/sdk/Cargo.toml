[package]
name = "openuuyc-plugin-api"
version = "0.1.0"
edition = "2024"
publish = false
license-file = "../../LICENSE"

[lib]
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
